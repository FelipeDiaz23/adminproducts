rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Modo de prueba: cualquiera con el link puede subir/leer fotos del catálogo.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
